import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
  <section>
    <app-home></app-home>
  </section>
  `,
})
export class AppComponent {
  title = 'portfolio-website';
}
